Never rewrite working components.

Never rename files without updating imports.

Never create duplicate hooks.

Maximum file length: 500 lines.

Maximum component length: 300 lines.

Never use any.

Always reuse existing UI components.

Always check the architecture before coding.

Always update TASKS.md after finishing.