Every time we make an architectural decision, we'll record:

Decision
Reason
Alternatives considered
Date
Impact

For example:

Why we chose HashRouter instead of BrowserRouter.
Why we use Zustand instead of Redux.
Why the workspace is built before the file explorer.